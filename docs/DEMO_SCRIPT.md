# Final Judge Demo Script

Target: **4:30–5:00 minutes**.

The judge should understand one thing immediately: **AI does not control money.**

## Before recording

- Confirm `/overview`, `/intelligence`, `/autopilot`, and `/audit` load.
- Do not create another hosted optimization cycle just to make the demo prettier.
- The hosted history contains both older simulated `demo_plink_*` resources and a later application-recorded Razorpay Test Mode resource: `plink_TW3blQWQpXXHRL`.
- If you can independently open that same `plink_*` in the Razorpay Test Mode dashboard/API, capture it for the demo. Do not show credentials.
- For the strongest create→fetch→ROLLBACK→cancel proof, run `python scripts/verify_razorpay_autopilot.py` locally with `rzp_test_*` credentials. Claim that full proof only after the script returns `PASS`.

See `docs/RAZORPAY_TEST_MODE_PROOF.md`.

---

## 0:00–0:30 — product + safety thesis

Open:

```text
https://merchant-revenue-autopilot-psi.vercel.app/overview
```

Say:

> "Razorpay already processes the merchant's payments. Revenue Autopilot uses that payment evidence to discover where conversion is leaking and safely experiments with payment configurations to improve it. AI proposes, deterministic policy authorizes, the execution boundary acts, fixed-horizon statistics decides, and persisted outcomes shape the next experiment."

Do not begin with model names, architecture diagrams, or test counts.

---

## 0:30–1:05 — evidence first

On **Overview**, point to conversion, captured GMV, payment attempts, weakest segment, and payment-method evidence.

Say:

> "The system starts from payment data, not a chat prompt. Metrics, divergence detection, and opportunity prioritization are deterministic. TechBazaar keeps experiment traffic across cycles, so the live accumulated attempt count is larger than the frozen 6,112-attempt benchmark baseline."

Then:

> "A merchant can also be created from canonical CSV history and append later transaction revisions with deduplication. Uploaded merchant evidence is kept separate from TechBazaar's sealed causal evaluation world."

Do not spend demo time uploading a CSV unless asked.

---

## 1:05–1:50 — AI enters only after observed evidence

Use the latest cycle:

```text
https://merchant-revenue-autopilot-psi.vercel.app/autopilot/2956c570-9504-40b6-9557-372fe7455ccc
```

Point to **Observed Evidence** first.

Say:

> "android_budget is around 47.2% conversion versus 58.6% for the comparison cohort. Everything above the AI boundary is deterministic merchant-visible evidence."

Move below **AI Analysis · Generated by LLM**.

Say:

> "Only now does the LLM enter. It receives evidence plus structured prior experiment memory and returns a schema-constrained causal hypothesis. It has no execution authority. Deterministic memory validation also prevents exact stale failed or inconclusive proposals from being repeated without material evidence change."

---

## 1:50–2:30 — plan + deterministic merchant policy

Point to **Experiment Plan** and **Policy Authorization**.

Say:

> "The planner converts the hypothesis into a bounded champion–challenger experiment. Champion v1 is still the merchant baseline because no hosted treatment has earned a statistically significant KEEP."

Then:

> "Merchant policy checks the intervention allow-list, treatment exposure, sample horizon, duration, discounts, financial exposure, and concurrent experiments. Unsafe proposals are rejected rather than silently rewritten."

Memorable line:

> "AI proposes; deterministic systems authorize."

---

## 2:30–3:15 — Razorpay execution proof, stated precisely

On the same latest cycle, point to **Razorpay Test Resource**.

The live product currently renders:

```text
plink_TW3blQWQpXXHRL
```

as a Razorpay Test Mode Payment Link created after policy `APPROVE`.

Say:

> "Earlier hosted cycles used an explicitly simulated adapter and produced `demo_plink_*` IDs. This later approved cycle persisted a plain `plink_*` and the product records it as a Razorpay Test Mode Payment Link. In this codebase the simulated adapter cannot generate that ID shape, so this is application-side evidence that the real Test Mode client path returned a Razorpay resource."

Then make the evidence boundary explicit:

> "Application-side evidence is not the same as independent external confirmation. If I show the matching object in the Razorpay Test Mode dashboard/API, that closes that verification gap."

### If you have independently confirmed `plink_TW3blQWQpXXHRL`

Show it for 10–15 seconds and say:

> "This is the same `plink_*` independently visible in Razorpay Test Mode."

### If you have not independently confirmed it

Do not fake the step. Say:

> "The application-side execution record is present; independent dashboard/API confirmation is the remaining proof step."

### If the controlled verifier has also PASSed

You may add:

> "I also ran the credential-gated proof harness: policy APPROVE → create → independent fetch → fixed-horizon ROLLBACK fixture → executor cancellation → independent cancellation fetch → verified audit chain."

Never show credentials and never call an older `demo_plink_*` a real Razorpay object.

---

## 3:15–3:50 — statistics, not the LLM, decides

On the latest cycle, point to **Statistical Result**.

Say:

> "This experiment reached its predefined horizon. Control converted at about 48.46%, treatment at 48.5%, lift was essentially zero, and p was about 0.9917. The result is INCONCLUSIVE. The LLM does not participate in that decision."

Then:

> "That is a feature, not a failure: the system refuses to promote its own idea without evidence. Champion v1 stays v1."

Do not call opportunity-sizing GMV realized revenue.

---

## 3:50–4:25 — learning system, not chat memory

Open:

```text
https://merchant-revenue-autopilot-psi.vercel.app/intelligence
```

Point to Champion v1, terminal trials, learned intervention history, and opportunity portfolio.

Say:

> "This is structured merchant learning reconstructed from persisted experiments, policy decisions, statistics, and execution state. Past outcomes affect future ranking, stale-repeat validation, and champion control."

Then:

> "Uploaded merchants can use their real historical evidence through the same reasoning layers. But the product refuses to manufacture their experiment lift from TechBazaar's simulator; it waits for authoritative real control and treatment outcomes."

---

## 4:25–4:50 — audit + close

Open **Audit Log**.

Point to **Integrity Verified** and the lifecycle events, including the later `plink_*` creation event.

Say:

> "Every major transition is appended to a SHA-256 hash-chained audit history: evidence, AI hypothesis, plan, policy, execution, and statistics. It is application-level tamper evidence, not a blockchain."

Close:

> "Revenue Autopilot is not a recommendation dashboard. It is a controlled learning loop around an AI growth agent: evidence in, bounded experiment out, deterministic authorization, measurable outcome, memory, and the next safer decision."

---

## Optional benchmark answer

If asked, "How do you know the strategy is useful?"

> "A separate frozen synthetic benchmark compares no optimization, random intervention, a rule baseline, and Autopilot over identical paired contexts. Autopilot averaged 59.39% conversion versus 58.18% for no optimization. That is reproducible synthetic evaluation evidence, not a production revenue claim."

---

## Current read-only production observation

Final smoke audit observed:

- 14,115 accumulated payment attempts
- Champion v1
- 5 terminal trials / 4 statistical results
- 0 promoted treatments
- 4 INCONCLUSIVE statistical results
- audit integrity Verified
- latest experiment `83b571f0-3520-4435-b279-7fad1f4e0efb`
- latest Test Mode resource `plink_TW3blQWQpXXHRL`
- latest statistical decision INCONCLUSIVE, p ≈ 0.9917

These are point-in-time live values, not frozen benchmark constants.

## Things not to claim

- Do not call a `demo_plink_*` resource real.
- Do not say the full external create→verify→cancel proof passed until it actually does.
- Do not treat the live `plink_*` application record as independent dashboard/API confirmation unless you show that confirmation.
- Do not call synthetic benchmark lift production merchant lift.
- Do not call opportunity-sizing GMV realized revenue.
- Do not say the LLM authorizes experiments or decides statistical success.
- Do not say Champion advanced; it remains v1.
- Do not imply uploaded merchants use TechBazaar's simulator for outcomes.
- Do not call the audit chain a blockchain.
- Do not lead with test count or system complexity.
